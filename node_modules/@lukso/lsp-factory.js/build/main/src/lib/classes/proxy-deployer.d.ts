import { Signer } from '@ethersproject/abstract-signer';
import { Contract } from '@ethersproject/contracts';
import { LSP1UniversalReceiverDelegateInit, UniversalProfileInit } from '../../';
export declare class ProxyDeployer {
    signer: Signer;
    baseContracts: {
        universalProfile: UniversalProfileInit;
        universalReceiverDelegate: LSP1UniversalReceiverDelegateInit;
    };
    constructor(signer: Signer);
    deployBaseContracts(): Promise<{
        universalProfile: UniversalProfileInit;
        universalReceiverDelegate: LSP1UniversalReceiverDelegateInit;
    }>;
    deployProxyContract<T extends Contract>(contract: T): Promise<Contract>;
}
